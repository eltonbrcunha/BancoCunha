@model IEnumerable<BancoCunha.Models.Banco>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Codbanco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nomebanco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Codbanco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nomebanco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idbanco">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idbanco">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idbanco">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
